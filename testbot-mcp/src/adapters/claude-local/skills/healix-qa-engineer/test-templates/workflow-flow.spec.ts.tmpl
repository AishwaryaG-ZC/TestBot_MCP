import { test, expect } from '@playwright/test';

test('[REQ:F1.S1.AC1] user completes source-grounded workflow', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('main')).toBeVisible();
});
